<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Confirmation Feedback</title>
  <style>
    body {
      font-family: Arial;
      background: #f9f9f9;
      padding: 2rem;
      text-align: center;
    }

    .card {
      background: white;
      padding: 2rem;
      border-radius: 8px;
      max-width: 600px;
      margin: auto;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }

    .btn {
      margin-top: 2rem;
      display: inline-block;
      padding: 0.5rem 1rem;
      background: #007bff;
      color: white;
      border: none;
      border-radius: 4px;
      text-decoration: none;
    }

    .btn:hover {
      background: #0056b3;
    }
  </style>
</head>
<body>

  <div class="card">
    <h2>✅ Merci pour votre feedback, <%= feedback.user.nom %> !</h2>
    <p>Votre avis a bien été enregistré pour :</p>
    <h3><%= feedback.product.nom %></h3>

    <div style="text-align: left; margin-top: 1.5rem;">
      <p><strong>Note :</strong> <%= feedback.note %>/5</p>
      <p><strong>Commentaire :</strong> <%= feedback.commentaire %></p>
      <p><strong>Satisfaction :</strong> <%= feedback.satisfaction %></p>
      <% if (feedback.dateAchat) { %>
        <p><strong>Date d'achat :</strong> <%= feedback.dateAchat.toLocaleDateString() %></p>
      <% } %>
      <% if (feedback.dureeUtilisation) { %>
        <p><strong>Durée d'utilisation :</strong> <%= feedback.dureeUtilisation %></p>
      <% } %>
    </div>

    <a href="/products" class="btn">⬅ Retour aux produits</a>
  </div>

</body>
</html>
